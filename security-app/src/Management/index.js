import React, { useEffect } from 'react';
import TaskPage from './TaskPage';

/* const fetchList = () => {
    return (list = getList());
}; */

const Management = () => {
    /*     useEffect(() => {
            fetchList();
        }); */

    return (
        <>
            <TaskPage />
        </>
    );
};

export default Management;
